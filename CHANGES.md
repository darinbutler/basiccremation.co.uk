# Batch 2.2 — Manchester template polish + lift content pages (FULL BUNDLE)

## Manchester / location-page.tsx (the template for all 36 city pages)

1. **'When the coroner is involved' — full page width**
   Was: narrow column (`max-w-prose mx-auto`). Now: 12-col grid with title-left/body-right, mirroring the existing About section.

2. **'Help with funeral costs' — full page width**
   Same fix. 12-col grid layout.

3. **Crematoria section — dark overlay with white headings**
   Background overlay changed from `bg-paper/88` (light) to `bg-sage-900/80` (dark). Heading text recoloured to `text-paper`, eyebrow to `text-coral-300`, body to `text-paper/85`, trailing italic note to `text-paper/70`. The previously illegible black-on-image text now reads as crisp white-on-dark — much more dramatic, properly legible.

4. **Bereavement support — brighter background image**
   `ADVICE_IMAGE` swapped from the previous mountain landscape to `photo-1470770841072-f978cf4d019e` ("bright sunlit lake and mountains") — the same image the homepage uses for its bright/optimistic block.

5. **Useful Guides — image background**
   Section now has a bright English countryside background image (`USEFUL_GUIDES_BG`) with a soft `bg-paper-warm/92` overlay so the cards still read as the focal point but the section feels designed, not flat.

## Other pages — image heros for engagement

The pages below previously used a flat `bg-paper-warm` hero. Now upgraded to image-bg heros matching the homepage / Manchester / what-is-a-basic-cremation pattern:

- **`/whats-included`** — misty country path hero
- **`/contact`** — bright English countryside hillside hero
- **`/faqs`** — lake and mountains hero

That brings the full set of content-page heros in line:
- Homepage ✓ (image hero)
- Prices ✓ (image hero, was already)
- What is a basic cremation ✓ (image hero, was already)
- Whats included ✓ (NEW image hero)
- FAQs ✓ (NEW image hero)
- Contact ✓ (NEW image hero)
- Help & advice (all 5) ✓ (image hero from Batch 2.1)

## Visual rhythm of every Manchester-style city page after this batch

1. Hero with image (existing)
2. In-page nav strip
3. About — 12-col title-left / body-right
4. Pricing — 2-col image+content
5. **Crematoria — dark dramatic overlay, white headings, paper cards** ⬅ refined
6. Areas covered — paper-warm strip
7. Process — 5-col coloured cards
8. Registering a death — 2-col content
9. **Coroner — full-width 12-col grid** ⬅ new
10. Bereavement support — **brighter** image background, paper cards ⬅ refined
11. **Funding — full-width 12-col grid** ⬅ new
12. FAQs — paper-warm strip
13. **Useful guides — bright background image** ⬅ refined
14. Final CTA — dark phone-CTA card

Six section types alternate (hero, content, image-bg-light, image-bg-dark, paper-warm, container-only) so the eye keeps moving instead of fatiguing on plain prose.
